#include <stdlib.h>
#include <string.h>
#include <stdio.h>

#include "Protocol.h"
#include "Socket.h"

/**
int main(){
	// sendto(sock, response, sizeof(response), 0, (struct sockaddr *)&sender, sendsize);
	return 0;
}
/**/

